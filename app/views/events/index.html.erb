<h2>地震應變中心</h2>
<%= link_to "地震災情",events_path%>
<%= link_to "新增災情",new_event_path%>
<% @events.each do |e|%>
  <%= link_to e.name, event_path(e)%>
  <%= e.category.try(:name)%>
  <%= e.created_at%>
  <%= link_to "刪除", event_path(e),:method => :delete%>
<% end %>

<%= paginate @events%>